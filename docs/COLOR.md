# IColor Model

## Properties

- **name** (`string`): The name of the color.
- **color** (`string `): The color of the cable.

## Example Usage

```typescript
const exampleColor: IColor = {
  name: 'Cable name',
  color: 'Blue',
};
```
